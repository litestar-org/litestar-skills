#!/usr/bin/env bash
# litestar-skills — multi-host installer
#
# v0.0.1 scaffold: full implementation lands in Saga 6 (installer-and-release-automation).
# This stub exists so bump-my-version has a target file to update.

set -euo pipefail

VERSION="0.0.1"

echo "litestar-skills installer v${VERSION}"
echo "Full multi-host install support coming in Saga 6 of v0.1-launch."
echo "For now, see README.md for per-host install commands."
